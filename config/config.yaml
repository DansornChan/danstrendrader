# ═══════════════════════════════════════════════════════════════
#                    TrendRadar 配置文件
# ═══════════════════════════════════════════════════════════════


# ===============================================================
# 1. 基础设置
# ===============================================================
app:
  # 时区配置（影响所有时间显示、推送窗口判断、数据存储）
  timezone: "Asia/Shanghai"
  show_version_update: true           # 显示版本更新提示


# ===============================================================
# 2. 数据源 - 热榜平台
# ===============================================================
platforms:
  enabled: true                         # 是否启用热榜平台抓取
  sources:
    - id: "toutiao"
      name: "今日头条"
    - id: "wallstreetcn-hot"
      name: "华尔街见闻"
    - id: "thepaper"
      name: "澎湃新闻"
    - id: "cls-hot"
      name: "财联社热门"
    - id: "ifeng"
      name: "凤凰网"
    - id: "gelonghui"
      name: "格隆汇"

    - id: "jin10"
      name: "金十数据"

    - id: "fastbull"
      name: "快讯通"

    - id: "fastbull-express"
      name: "法布财经 快讯"

    - id: "fastbull-news"
      name: "法布财经 头条"

    - id: "tencent-hot"
      name: "腾讯新闻 综合早报"


# ===============================================================
# 3. 数据源 - RSS 订阅
# ===============================================================
rss:
  enabled: true                       # 是否启用 RSS 抓取

  freshness_filter:
    enabled: true                     # 是否启用新鲜度过滤（默认启用）
    max_age_days: 3                   # 最大文章年龄（天）

  feeds:
    - id: "hacker-news"
      name: "Hacker News"
      url: "https://hnrss.org/frontpage"
    
    - id: "yahoo-finance"
      name: "雅虎财经"
      url: "https://finance.yahoo.com/news/rssindex"
      enabled: false                  # 禁用


# ===============================================================
# 4. 报告模式
# ===============================================================
report:
  mode: "current"                     # 可选: daily | current | incremental
  display_mode: "keyword"             # 分组维度: keyword | platform

  # 关键词组排序方式（仅 display_mode: keyword 时生效）
  sort_by_position_first: false

  rank_threshold: 5                   # 排名高亮阈值

  max_news_per_keyword: 3             # 每个关键词最大显示数量（0=不限制）


# ===============================================================
# 5. 推送内容控制
# ===============================================================
display:
  # 📋 区域显示顺序
  region_order:
    - new_items                       # 1️⃣ 新增热点区域
    - hotlist                         # 2️⃣ 热榜区域（关键词匹配）
    - standalone                      # 4️⃣ 独立展示区
    - ai_analysis                     # 5️⃣ AI 分析区域

  # 推送区域开关
  regions:
    hotlist: true                     # 热榜区域
    new_items: true                   # 新增热点区域
    rss: true                         # RSS 订阅区域
    standalone: false                 # 独立展示区
    ai_analysis: true                 # AI 分析区域

  # 📋 独立展示区配置
  standalone:
    platforms: []                     # 热榜平台 ID 列表
    rss_feeds: []                     # RSS 源 ID 列表
    max_items: 20                     # 每个源最多展示条数


# ===============================================================
# 6. 推送通知
# ===============================================================
notification:
  enabled: true                       # 是否启用通知功能

  # 🕐 推送时间窗口控制
  push_window:
    enabled: false                    # 是否启用推送时间窗口控制
    start: "20:00"                    # 开始时间（北京时间）
    end: "22:00"                      # 结束时间（北京时间）
    once_per_day: true                # true=窗口内只推送一次

  # 推送渠道配置
  channels:
    feishu:
      webhook_url: ""                 # 飞书机器人 webhook URL

    dingtalk:
      webhook_url: ""                 # 钉钉机器人 webhook URL

    wework:
      webhook_url: ""                 # 企业微信机器人 webhook URL
      msg_type: "markdown"            # 消息类型

    telegram:
      bot_token: "TELEGRAM_BOT_TOKEN"
      chat_id: "TELEGRAM_CHAT_ID"

    email:
      from: ""                        # 发件人邮箱地址
      password: ""                    # 发件人邮箱密码或授权码
      to: ""                          # 收件人邮箱
      smtp_server: ""
      smtp_port: ""

    ntfy:
      server_url: "https://ntfy.sh"
      topic: ""
      token: ""

    bark:
      url: ""                         # Bark 推送 URL

    slack:
      webhook_url: ""                 # Slack Incoming Webhook URL

    generic_webhook:
      webhook_url: ""
      payload_template: ""


# ===============================================================
# 7. 存储配置
# ===============================================================
storage:
  backend: "auto"

  formats:
    sqlite: true                      # 主存储（必须启用）
    txt: false                        # 是否生成 TXT 快照
    html: true                        # 是否生成 HTML 报告

  local:
    data_dir: "output"
    retention_days: 0

  remote:
    retention_days: 0
    endpoint_url: ""
    bucket_name: ""
    access_key_id: ""
    secret_access_key: ""
    region: ""

  pull:
    enabled: false
    days: 7


# ===============================================================
# 8. AI 模型配置（共享）
#
# ai_analysis 和 ai_translation 共用此模型配置
# ===============================================================
ai:
  # 1. 主力模型：Google Gemini 2.5 Flash
  # -------------------------------------------------------------
  model: "gemini/gemini-2.5-flash"

  # 主力模型的 Key
  # 程序会自动读取环境变量中的 GEMINI_API_KEY
  api_key: "GEMINI_API_KEY"

  # 2. 备用模型：DeepSeek V3 (关键修改)
  # -------------------------------------------------------------
  # 当主力模型报错 (如 429 配额超限) 时，自动切换到 DeepSeek。
  # 只要你在 GitHub Secrets 设置了 DEEPSEEK_API_KEY，这里不需要填 Key。
  fallback_models: 
    - "deepseek/deepseek-chat"

  # 3. 通用参数
  api_base: ""                      # 留空即可
  timeout: 120                      # 请求超时（秒）
  temperature: 1.0                  # 采样温度
  max_tokens: 5000                  # 最大生成 token 数
  num_retries: 1                    # 失败重试次数


# ===============================================================
# 9. AI 分析功能
# ===============================================================
ai_analysis:
  enabled: true                     # 是否启用 AI 分析
  language: "Chinese"
  prompt_file: "ai_analysis_prompt.txt"

  max_news_for_analysis: 50         # 参与分析的新闻数量上限
  include_rss: false                # 是否包含 RSS 内容进行分析
  include_rank_timeline: true       # 是否传递完整排名时间线


# ===============================================================
# 10. AI 翻译功能
# ===============================================================
ai_translation:
  enabled: false                    # 是否启用翻译功能
  language: "English"
  prompt_file: "ai_translation_prompt.txt"


# ===============================================================
# 11. 高级设置（一般无需修改）
# ===============================================================
advanced:
  debug: false
  version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version"
  mcp_version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version_mcp"

  crawler:
    request_interval: 2000
    use_proxy: false
    default_proxy: "http://127.0.0.1:10801"

  rss:
    request_interval: 1000
    timeout: 15
    use_proxy: false
    proxy_url: ""

  weight:
    rank: 0.6
    frequency: 0.3
    hotness: 0.1

  max_accounts_per_channel: 3

  batch_size:
    default: 4000
    dingtalk: 20000
    feishu: 30000
    bark: 4000
    slack: 4000
  batch_send_interval: 3
  feishu_message_separator: "━━━━━━━━━━━━━━━━━━━"
